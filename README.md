# Windows Server API Example

Based off of: https://learn.microsoft.com/en-us/windows/win32/http/http-server-sample-application

`http.c` and `mime.c` are the relevant files.

Look at `example.c` for usage.
